apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: feheroes
  namespace: default
  annotations: 
    build.knative.dev/docker-0: https://index.docker.io/v1/
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: docker.io/egsmartin/fehello-jib:0.0.1-SNAPSHOT
            imagePullPolicy: Always
            ports:
              - containerPort: 8082